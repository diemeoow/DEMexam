<Window x:Class="WpfApp1.ProductForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="ProductForm" Height="650" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Каталог товаров" FontSize="20" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Панель управления - АДАПТИВНАЯ С RESPONSIVE WRAPPANEL -->
        <Border Grid.Row="1" Background="#F8F9FA" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <!-- Первая строка -->
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0" MinWidth="50"/>
                    <TextBox x:Name="SearchTextBox" Width="140" MinWidth="100"
                        
                        Padding="3" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="0,0,5,0" MinWidth="70"/>
                    <ComboBox x:Name="SortComboBox" Width="120" MinWidth="80">
                        <ComboBoxItem Content="Название (А-Я)"/>
                        <ComboBoxItem Content="Название (Я-А)"/>
                        <ComboBoxItem Content="Цена (↑)"/>
                        <ComboBoxItem Content="Цена (↓)"/>
                        <ComboBoxItem Content="Количество"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Категория:" VerticalAlignment="Center" Margin="0,0,5,0" MinWidth="70"/>
                    <ComboBox x:Name="CategoryFilterComboBox" Width="120" MinWidth="80"
                         
                         DisplayMemberPath="Name"/>
                </StackPanel>

                <!-- Вторая строка -->
                <Button x:Name="AddButton" Content="Добавить" 
                   Background="#007BFF" Foreground="White" Padding="8,4" Margin="5" />
                
            </StackPanel>
        </Border>

        <!-- Контейнер для карточек -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
  
                
            <ItemsControl x:Name="ProductsItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:TovarCard/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
    
        </ScrollViewer>

        <!-- Статус -->
        <TextBlock Grid.Row="3" x:Name="StatusText" Text="" FontSize="12" Foreground="Gray" 
              Margin="0,10,0,0" HorizontalAlignment="Center"/>
    </Grid>
</Window>
