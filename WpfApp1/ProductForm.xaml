<Window x:Class="WpfApp1.ProductForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="ProductForm" Height="650" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Background="#F8F9FA" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">

                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0" MinWidth="50"/>
                    <TextBox x:Name="SearchTextBox" Width="140" MinWidth="100" TextChanged="SearchTextBox_TextChanged"
                        Padding="3" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="0,0,5,0" MinWidth="70"/>
                    <Button x:Name="SortButton" Content="По убыванию" Click="SortButton_Click" Margin="10,0,0,0" Padding="10,5"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Поставщик:" VerticalAlignment="Center" Margin="0,0,5,0" MinWidth="70"/>
                    <ComboBox x:Name="SupplierFilterComboBox" Width="120" MinWidth="80"
                         SelectionChanged="SupplierFilterComboBox_SelectionChanged"
                         DisplayMemberPath="Name"/>
                </StackPanel>
                <Button x:Name="AddTovar" Content="Добавить" Click="AddButton_Click" Margin="10,5,0,5" Padding="10,5"/>
            </StackPanel>
        </Border>

        <!-- Контейнер для карточек -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProductsItemsControl">
                
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                
                    <ItemsControl.ItemTemplate>
                     <DataTemplate>
                         <local:TovarCard/>
                     </DataTemplate>
                 </ItemsControl.ItemTemplate>
                
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</Window>
